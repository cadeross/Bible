/** When true, shows the global database maintenance strip at the top of the app. */
export const DATABASE_MAINTENANCE_BANNER_ENABLED =
    process.env.NEXT_PUBLIC_DATABASE_MAINTENANCE_BANNER === "true"
